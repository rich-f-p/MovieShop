@model IEnumerable<Movie>;
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <partial name="_MovieCardModel" , model="@item.PosterUrl" />
        }
    </div>
</div>